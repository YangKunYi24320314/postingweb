# 发现记录

- 当前仓库位于 `C:\Users\张sir\OneDrive\桌面\作业\postingweb`，远端为 `origin`。
- 当前分支为 `main`，工作区已有 package-lock 修改，不能覆盖。
- PostgreSQL 已安装在 `D:\PostgreSQL\17`，数据库 `campushub` 可用。
- 用户要求每天晚上 20:00 前上传，开机补跑时当天成功上传后必须跳过重复上传。
- 用户说明接口文档在 Apifox；本地仓库仍有 `devdocs` 契约文档可作为实现基线。
- 2026-09-03：已从 `origin/develop` 获取最新提交；整体合并会带入不属于本分支的帖子、评论、互动、分类标签实现，因此只同步契约文档提交 `54ee4c6`。
- 2026-09-03：账号安全设计确定为阿里云短信 + SMTP 邮件；验证码 6 位、5 分钟有效、60 秒冷却、最多 5 次尝试，验证码只保存哈希。
- 2026-09-03：本次实现安全检查点为 `20260903182548822-before-contact-auth`。
- 2026-09-03：真实 provider 依赖为 `@alicloud/dysmsapi20170525`、`@alicloud/openapi-client` 和 `nodemailer`；缺少 `.env` 配置时服务仍可启动，但调用对应发送渠道会返回通用 500 错误。
- 2026-09-03：用户明确要求先审阅再上传，因此本次不执行 `git push`。
- 2026-09-03：根因确认是注册态复用登录标识输入框且后端未拒绝联系方式用户名；已修复为“先注册用户名，再绑定联系方式，绑定后用于登录”。
