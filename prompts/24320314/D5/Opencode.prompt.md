# 项目背景与角色设定

> 本提示词用于开启一个新的 AI 协作会话，帮助 AI 快速理解项目全貌并明确本次任务。


## 本次会话任务（按顺序执行）

1. **重做「项目介绍」独立静态页 `client/public/about.html` 的框架**：
   - AI 只负责搭建页面**骨架与结构**：在 `:root` 复刻 `tokens.css` 的全部设计变量，并给出 hero 区、功能模块区、技术栈区、关于本项目区、源码与文档区、footer 等布局与样式类，保证风格与整个前端一致。
   - **正文文案（标题、介绍文字、模块描述、链接文本等）一律使用占位/示例文本**，留待我（PM）手工填写真实内容，AI 不要代写正式文案。
   - 保持为独立静态页，可经 `/about.html` 直接访问；首页入口用整页跳转 `window.location.href = '/about.html'`（静态页走不了 `vue-router`）。

2. **处理 PR**：
   - 我（YangKunYi24320314）负责 `develop` 分支的 PR 申请处理（审查 / 合并）。
   - 处理*******的冲突，提出修改意见。
## 注意事项

- Git 提交（commit / push）由我本人自行完成，你无需执行，后续也不需要询问是否提交。
- 涉及环境变量 / 连接配置时，主机地址、密码等敏感信息以会话中实际提供为准。
