# D5 日报

> 日期：2026-09-05（第 5 天）

## 昨日遗留问题的回答

（第 4 天遗留：补「他人主页」页面入口、整体联调。今日持续推进个人主页 / 记录中心优化与新功能，详见下。）

## 今日目标

- 从 develop 拉新分支，继续个人主页 / 记录中心的优化与美化；
- 完成个人主页与「他人主页」的系列 UI 优化（页签、附件预览、动效、悬停交互、搜索）；
- 记录中心浏览记录支持单条删除；
- 增加管理员身份标识。

## 实际进展

**Git 协作**
- 从最新 develop 依次拉取并完成多条功能分支：`feature/security-center`（安全中心入口）、`develop-profileBeatify` / `develop-profileBeautiful2`（个人主页美化）、`develop-profileAdmin`（管理员标签），多次 push + 提 PR 合入 develop。

**个人主页 / 他人主页**
- 「···」菜单新增「个人安全中心」入口，并新建 `SecurityCenterView.vue` 框架页（留给认证模块队友开发）。
- 「我发布的」页签加粗放大。
- 新增 `PostMediaPreview.vue` 组件：卡片内展示图片/视频附件缩略图（最多 2 张，超出显示主题色「+」框跳详情；图片用 el-image 预览、视频首帧 + 播放）。
- 个人信息卡动效：尝试「扫光」后，最终回退为品牌蓝渐变流动，并微调透明度 / 背景图不透明度。
- 最近浏览预览、记录中心表格：悬停行文字变品牌色并左移。
- 「Ta的投稿」新增关键词模糊搜索（标题/正文，防抖 300ms）。

**记录中心**
- 浏览记录支持单条删除：悬停行显示「×」按钮，点击弹确认框后删除；
- 「清空浏览记录」也改为 ElMessageBox 确认弹窗。

**管理员标识**
- 个人主页 / 他人主页昵称右侧：`role === 'admin'` 时显示主题色胶囊「管理员」标签；后端 `/users/:id` 补充返回 `role`。

## 遇到的问题与解决

1. **本地数据库缺 `phone`/`email` 等列** → 生成幂等 SQL（`ADD COLUMN IF NOT EXISTS` + `verification_codes` 表）补库，重启后端解决。
2. **背景图颜色偏浅** → 将背景图不透明度从 0.6 调至 0.85（后按需求回退到 main 效果 0.6）。
3. **动效不明显 / 观感反复** → 先做「扫光」，经多轮微调后确认偏好，最终回退为品牌蓝渐变流动。
4. **提 PR 前有冲突风险** → 梳理 7 个改动文件的冲突面（契约文档为高风险），并提示队友合并后需重启后端。

## 明日计划

- 整体联调、修复遗留问题；
- 准备答辩讲稿与演示。
